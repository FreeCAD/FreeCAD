# SPDX-License-Identifier: LGPL-2.1-or-later

"""Import all Import module GUI unit tests.

This file only imports GUI test classes from the `importtests` package so the
test runner can discover them from the module root, mirroring the pattern
used by the BIM module's `TestArchGui.py`.
"""

from importtests.TestImportStepGui import TestImportStepGui

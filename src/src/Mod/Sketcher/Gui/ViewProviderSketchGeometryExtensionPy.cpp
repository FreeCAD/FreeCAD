
// This file is generated by src/Tools/generateTemplates/templateClassPyExport.py out of the .XML file
// Every change you make here gets lost in the next full rebuild!
// This File is normally built as an include in ViewProviderSketchGeometryExtensionPyImp.cpp! It's not intended to be in a project!

#include <Base/PyObjectBase.h>
#include <Base/Console.h>
#include <Base/Exception.h>
#include <CXX/Objects.hxx>

#if defined(__clang__)
# pragma clang diagnostic push
# pragma clang diagnostic ignored "-Wdeprecated-declarations"
#endif

using Base::streq;
using namespace SketcherGui;

#if defined(__GNUC__)
#pragma GCC diagnostic push
// Ignore -Wmissing-field-initializers (GCC only):
// - C++20 guarantees omitted fields are zero-initialized.
// - Python C API changes fields across versions.
// - Clang does not warn; GCC does unnecessarily.
// See https://gcc.gnu.org/bugzilla/show_bug.cgi?id=96868
#pragma GCC diagnostic ignored "-Wmissing-field-initializers"
#endif // __GNUC__
/// Type structure of ViewProviderSketchGeometryExtensionPy
PyTypeObject ViewProviderSketchGeometryExtensionPy::Type = {
    .ob_base = PyVarObject_HEAD_INIT(&PyType_Type,0)
    .tp_name = "SketcherGui.ViewProviderSketchGeometryExtension",
    .tp_basicsize = sizeof(ViewProviderSketchGeometryExtensionPy),
    .tp_itemsize = 0,
    /* methods */
    .tp_dealloc = PyDestructor,
    .tp_repr = __repr,
    .tp_getattro = __getattro,
    .tp_setattro = __setattro,
    /* --- Flags to define presence of optional/expanded features */
    .tp_flags = Py_TPFLAGS_BASETYPE|Py_TPFLAGS_DEFAULT,
    .tp_doc = "Describes a ViewProviderSketchGeometryExtension",
    .tp_methods = SketcherGui::ViewProviderSketchGeometryExtensionPy::Methods,
    .tp_getset = SketcherGui::ViewProviderSketchGeometryExtensionPy::GetterSetter,
    .tp_base = &Part::GeometryExtensionPy::Type,
    .tp_init = __PyInit,
    .tp_new = SketcherGui::ViewProviderSketchGeometryExtensionPy::PyMake
};
#if defined(__GNUC__)
#pragma GCC diagnostic pop
#endif // __GNUC__

/// Methods structure of ViewProviderSketchGeometryExtensionPy
PyMethodDef ViewProviderSketchGeometryExtensionPy::Methods[] = {
    {nullptr, nullptr, 0, nullptr}		/* Sentinel */
};



/// Attribute structure of ViewProviderSketchGeometryExtensionPy
PyGetSetDef ViewProviderSketchGeometryExtensionPy::GetterSetter[] = {
    {"VisualLayerId",
        (getter) staticCallback_getVisualLayerId,
        (setter) staticCallback_setVisualLayerId,
        "Sets/returns this geometry's Visual Layer Id.",
        nullptr
    },
    {nullptr, nullptr, nullptr, nullptr, nullptr}		/* Sentinel */
};

// VisualLayerId() callback and implementer
// PyObject*  ViewProviderSketchGeometryExtensionPy::VisualLayerId(PyObject *args){};
// has to be implemented in ViewProviderSketchGeometryExtensionPyImp.cpp
PyObject * ViewProviderSketchGeometryExtensionPy::staticCallback_getVisualLayerId (PyObject *self, void * /*closure*/)
{
    if (!static_cast<PyObjectBase*>(self)->isValid()){
        PyErr_SetString(PyExc_ReferenceError, "This object is already deleted most likely through closing a document. This reference is no longer valid!");
        return nullptr;
    }

    try {
        return Py::new_reference_to(static_cast<ViewProviderSketchGeometryExtensionPy*>(self)->getVisualLayerId());
    } catch (const Py::Exception&) {
        // The exception text is already set
        return nullptr;
    } catch (...) {
        PyErr_SetString(Base::PyExc_FC_GeneralError, "Unknown exception while reading attribute 'VisualLayerId' of object 'ViewProviderSketchGeometryExtension'");
        return nullptr;
    }
}

int ViewProviderSketchGeometryExtensionPy::staticCallback_setVisualLayerId (PyObject *self, PyObject *value, void * /*closure*/)
{
    if (!static_cast<PyObjectBase*>(self)->isValid()){
        PyErr_SetString(PyExc_ReferenceError, "This object is already deleted most likely through closing a document. This reference is no longer valid!");
        return -1;
    }
    if (static_cast<PyObjectBase*>(self)->isConst()){
        PyErr_SetString(PyExc_ReferenceError, "This object is immutable, you can not set any attribute or call a method");
        return -1;
    }

    try {
        static_cast<ViewProviderSketchGeometryExtensionPy*>(self)->setVisualLayerId(Py::Long(value,false));
        return 0;
    } catch (const Py::Exception&) {
        // The exception text is already set
        return -1;
    } catch (...) {
        PyErr_SetString(Base::PyExc_FC_GeneralError, "Unknown exception while writing attribute 'VisualLayerId' of object 'ViewProviderSketchGeometryExtension'");
        return -1;
    }
}




//--------------------------------------------------------------------------
// Constructor
//--------------------------------------------------------------------------
ViewProviderSketchGeometryExtensionPy::ViewProviderSketchGeometryExtensionPy(ViewProviderSketchGeometryExtension *pcObject, PyTypeObject *T)
    : GeometryExtensionPy(static_cast<GeometryExtensionPy::PointerType>(pcObject), T)
{
}


//--------------------------------------------------------------------------
// destructor
//--------------------------------------------------------------------------
ViewProviderSketchGeometryExtensionPy::~ViewProviderSketchGeometryExtensionPy()                                // Everything handled in parent
{
}

//--------------------------------------------------------------------------
// ViewProviderSketchGeometryExtensionPy representation
//--------------------------------------------------------------------------
PyObject *ViewProviderSketchGeometryExtensionPy::_repr()
{
    return Py_BuildValue("s", representation().c_str());
}

//--------------------------------------------------------------------------
// ViewProviderSketchGeometryExtensionPy Attributes
//--------------------------------------------------------------------------
PyObject *ViewProviderSketchGeometryExtensionPy::_getattr(const char *attr)			// __getattr__ function: note only need to handle new state
{
    try {
        // getter method for special Attributes (e.g. dynamic ones)
        PyObject *r = getCustomAttributes(attr);
        if(r) return r;
    } // Please sync the following catch implementation with PY_CATCH
    catch(const Base::Exception& e)
    {
        e.setPyException();
        return nullptr;
    }
    catch(const std::exception& e)
    {
        PyErr_SetString(Base::PyExc_FC_GeneralError, e.what());
        return nullptr;
    }
    catch(const Py::Exception&)
    {
        // The exception text is already set
        return nullptr;
    }
#ifndef DONT_CATCH_CXX_EXCEPTIONS
    catch(...)
    {
        PyErr_SetString(Base::PyExc_FC_GeneralError,"Unknown C++ exception");
        return nullptr;
    }
#endif

    PyMethodDef *ml = Methods;
    for (; ml->ml_name != nullptr; ml++) {
        if (attr[0] == ml->ml_name[0] &&
            strcmp(attr+1, ml->ml_name+1) == 0)
            return PyCFunction_New(ml, this);
    }

    PyErr_Clear();
    return GeometryExtensionPy::_getattr(attr);
}

int ViewProviderSketchGeometryExtensionPy::_setattr(const char *attr, PyObject *value) // __setattr__ function: note only need to handle new state
{
    try {
        // setter for special Attributes (e.g. dynamic ones)
        int r = setCustomAttributes(attr, value);
        // r = 1: handled
        // r = -1: error
        // r = 0: ignore
        if (r == 1)
            return 0;
        else if (r == -1)
            return -1;
    } // Please sync the following catch implementation with PY_CATCH
    catch(const Base::Exception& e)
    {
        e.setPyException();
        return -1;
    }
    catch(const std::exception& e)
    {
        PyErr_SetString(Base::PyExc_FC_GeneralError, e.what());
        return -1;
    }
    catch(const Py::Exception&)
    {
        // The exception text is already set
        return -1;
    }
#ifndef DONT_CATCH_CXX_EXCEPTIONS
    catch(...)
    {
        PyErr_SetString(Base::PyExc_FC_GeneralError, "Unknown C++ exception");
        return -1;
    }
#endif

    return GeometryExtensionPy::_setattr(attr, value);
}

ViewProviderSketchGeometryExtension *ViewProviderSketchGeometryExtensionPy::getViewProviderSketchGeometryExtensionPtr() const
{
    return static_cast<ViewProviderSketchGeometryExtension *>(_pcTwinPointer);
}

#if defined(__clang__)
# pragma clang diagnostic pop
#endif

#if 0
/* From here on come the methods you have to implement, but NOT in this module. Implement in ViewProviderSketchGeometryExtensionPyImp.cpp! This prototypes
 * are just for convenience when you add a new method.
 */

PyObject *ViewProviderSketchGeometryExtensionPy::PyMake(PyTypeObject* /*type*/, PyObject* /*args*/, PyObject* /*kwds*/)
{
    // create a new instance of ViewProviderSketchGeometryExtensionPy and the Twin object
    return new ViewProviderSketchGeometryExtensionPy(new ViewProviderSketchGeometryExtension);
}

// constructor method
int ViewProviderSketchGeometryExtensionPy::PyInit(PyObject* /*args*/, PyObject* /*kwd*/)
{
    return 0;
}


// returns a string which represents the object e.g. when printed in python
std::string ViewProviderSketchGeometryExtensionPy::representation() const
{
    return {"<ViewProviderSketchGeometryExtension object>"};
}



Py::Long ViewProviderSketchGeometryExtensionPy::getVisualLayerId() const
{
    //return Py::Long();
    throw Py::AttributeError("Not yet implemented");
}

void  ViewProviderSketchGeometryExtensionPy::setVisualLayerId(Py::Long arg)
{
    throw Py::AttributeError("Not yet implemented");
}

PyObject *ViewProviderSketchGeometryExtensionPy::getCustomAttributes(const char* /*attr*/) const
{
    return nullptr;
}

int ViewProviderSketchGeometryExtensionPy::setCustomAttributes(const char* /*attr*/, PyObject* /*obj*/)
{
    return 0;
}


#endif




SUBDIRS=App Gui

# Change data dir from default ($(prefix)/share) to $(prefix)
datadir = $(prefix)/Mod/_TEMPLATE_
data_DATA = Init.py InitGui.py

EXTRA_DIST = \
		$(data_DATA) \
		CMakeLists.txt \
		_TEMPLATE_.dox

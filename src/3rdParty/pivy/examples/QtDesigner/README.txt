The same GUI as in ../examiner_embed*, but designed in Qt Designer.
The cpp and python folders show the same example implemented in C++ and Python respectively.

Python note:
test_ui.py is generated by running "pyuic ../test.ui > test_ui.py"

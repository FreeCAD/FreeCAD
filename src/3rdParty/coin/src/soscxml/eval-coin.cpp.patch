NOTE: This patch seems to be obsolete, it currently is unused.

Index: eval-coin.cpp
===================================================================
--- eval-coin.cpp	(revision 12928)
+++ eval-coin.cpp	(working copy)
@@ -725,7 +725,7 @@
 	if ( YY_CURRENT_BUFFER_LVALUE->yy_is_interactive ) \
 		{ \
 		int c = '*'; \
-		int n; \
+		unsigned int n; \
 		for ( n = 0; n < max_size && \
 			     (c = getc( scxml_coin_in )) != EOF && c != '\n'; ++n ) \
 			buf[n] = (char) c; \

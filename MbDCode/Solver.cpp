#include <assert.h>

#include "Solver.h"

void MbD::Solver::initialize()
{
	assert(false);
}

void MbD::Solver::initializeLocally()
{
	assert(false);
}

void MbD::Solver::initializeGlobally()
{
	assert(false);
}

void MbD::Solver::run()
{
	assert(false);
}

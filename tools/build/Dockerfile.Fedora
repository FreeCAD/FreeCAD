FROM fedora:latest

ENV TZ=Etc/UTC
ENV UTC=true
ENV ARC=false

COPY fedora.sh /tmp

RUN dnf update --assumeyes && \
    sh /tmp/fedora.sh && \
    mkdir /builds

WORKDIR /builds

VOLUME [ "/builds" ]

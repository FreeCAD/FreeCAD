name: freecad
channels:
- conda-forge
dependencies:
- conda-devenv
- mamba==1.5.6    # NOTE: Pin to highest version supported by the devenv
- python==3.11.*  # dependencies. If a higher version is installed, a crash
                  # occurs during the downgrade process.
